# GearX's Data Analysis Tool

- MSSQL
- NodeJS
- React Hook
- Material UI
- Axios

# Setup

- Clone this reposittory into `<forder>`.
- `cd <folder> && yarn add`
- `yarn start` to run the NodeJS server.
- `cd db-gear && yarn add`
- `yarn start` to run App UI.

# Confirmation
- Bui Ba Anh